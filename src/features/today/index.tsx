import { TabPlaceholder } from '../../shared/ui/TabPlaceholder';

export function TodayPlaceholder() {
  return (
    <TabPlaceholder
      title="Your day, held gently"
      description="The Today loop will live here: schedule, recording, processing, and journal readiness."
      accessibilityLabel="Today foundation screen"
    />
  );
}
