import { TabPlaceholder } from '../../shared/ui/TabPlaceholder';

export function SettingsPlaceholder() {
  return (
    <TabPlaceholder
      title="Make Daytale yours"
      description="Schedule, language, permissions, privacy, and data controls will live here."
      accessibilityLabel="Settings foundation screen"
    />
  );
}
